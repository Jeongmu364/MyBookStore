<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CartProductInfoDao">
    <insert id="insertProductInfo">
        insert
        into cart_product_info(cart_id, product_id, product_count)
        values (#{cartId}, #{productId}, #{productCount})
    </insert>

    <update id="updateProductCount">
        update cart_product_info
        set product_count = ${productCount}
        where id = ${id}
    </update>

    <delete id="deleteProductInfo">
        delete
        from cart_product_info
        where id = ${id}
    </delete>

    <select id="getProductInfoList">
        select id, cart_id, product_id, product_count
        from cart_product_info
        where cart_id = ${cartId}
    </select>
</mapper>